import { CommonColorDirective } from './common-color.directive';

describe('CommonColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonColorDirective();
    expect(directive).toBeTruthy();
  });
});
